name: Lean Action CI

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5
      - uses: leanprover/lean-action@v1
      - name: Run integration suite
        run: scripts/integration/run.sh
      - name: Run Lean test suite
        run: lake exe run_tests
